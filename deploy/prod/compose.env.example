# Docker Compose settings for the production stack.
COMPOSE_PROJECT_NAME=ai-sentinel
APP_ENV_FILE=deploy/prod/app.env
ASSISTANT_ENV_FILE=deploy/prod/assistant.env
LOGS_DIR=./logs/prod

API_CONTAINER_PORT=8080
API_HOST_PORT=8080
WEB_HOST_PORT=3000

POSTGRES_DB=ilyon_ai
POSTGRES_USER=sentinel
POSTGRES_PASSWORD=replace-with-a-strong-password

NEXT_PUBLIC_API_URL=https://ilyonai.com
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Internal rewrite targets used by the Next.js container
API_REWRITE_TARGET=http://api:8080
ASSISTANT_API_TARGET=http://assistant-api:8000
